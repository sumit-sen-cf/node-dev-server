<!DOCTYPE html>
<html>

<head>
  <title>Error Message</title>
  <link rel="icon" type="image/png" href="https://creativefuel.io/assets/img/image20.png">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins&display=swap');

    * {
      padding: 0;
      margin: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;

    }

    body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: url('https://www.heet.org.uk/wp-content/uploads/2016/06/gradient-background-26046-26731-hd-wallpapers.jpg.png');
            background-size: cover;
            background-position: center;
            position: relative;
            overflow: hidden;
        }

        body::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            bottom: 0;
            left: 0;
            background: url('https://images.hdqwalls.com/download/blur-background-6z-1280x720.jpg');
            background-size: cover;
            background-position: center;
            opacity: 0;
            animation: fadein 3s ease-in-out infinite alternate;
        }

        @keyframes fadein {
            0% {
                opacity: 0;
            }

            100% {
                opacity: 1;
            }
        }

    .wrapper {
      width: 420px;
      background: transparent;
      border: 2px solid rgba(255, 255, 255, .2);
      backdrop-filter: blur(20px);
      box-shadow: 0 0 10px rgba(0, 0, 0, .2);
      color: #fff;
      border-radius: 10px;
      padding: 37px 33px;
      text-align: center;
      max-width: 618px;
      width: 435px;
      animation: shake 0.5s ease-in-out 0s 1;
    }
    @keyframes shake {
        0%,
        100% {
          transform: translateX(0);
        }
        10%,
        30%,
        50%,
        70%,
        90% {
          transform: translateX(-10px);
        }
        20%,
        40%,
        60%,
        80% {
          transform: translateX(10px);
        }
      }

    .wrapper h1 {
      font-size: 36px;
      text-align: center;
    }

    .error-image {
      max-width: 100%;
      height: auto;
      margin-bottom: 20px;
    }




    .wrapper .remember-forgot {
      display: flex;
      justify-content: space-between;
      font-size: 14.5px;
      margin: -15px 0 15px;
    }

    .remember-forgot label input {
      accent-color: #fff;
      margin-right: 3px;
    }

    .remember-forgot a {
      color: #fff;
      text-decoration: none;

    }

    .remember-forgot a:hover {
      text-decoration: underline;
    }

    .wrapper .btn {
      width: 100%;
      height: 45px;
      border-radius: 40px;
      border: none;
      outline: none;
      background: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, .1);
      cursor: pointer;
      font-size: 16px;
      color: #333;
      font-weight: 600;
      margin-top: 10px;
    }
  </style>

</head>

<body>
  <div class="wrapper">
    <img src="<%= error_image %>" alt="Error Image" class="error-image" />
    <p class="error-message">
      <%= error_title %>
    </p>
    <p class="error-details">
      <%= error_description %>
    </p>
    <i class="fas fa-thumbs-up thank-you-icon"></i>
    <button onclick="location.href='<%= button_path %>';" class="btn">
      <%= button_text %>
    </button>
  </div>
</body>

</html>